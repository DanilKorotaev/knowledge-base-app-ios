# Document API client vs OpenAPI

**Status:** Pending

When KB App API is implemented, document:

- Exact paths and query params.
- Pagination and error envelopes.
- Auth header format.

Update `docs/ARCHITECTURE.md` and inline client code comments.
