// Copy this file to Secrets.xcconfig in the same folder (Secrets.xcconfig is gitignored).
// Prefer runtime configuration via Settings and Keychain for tokens.

// Example (optional): local display name
// INFOPLIST_KEY_CFBundleDisplayName = KB Local
