# Copy to .env (gitignored), fill in, then: ./scripts/sync-secrets-xcconfig.sh
# See docs/API_CONFIGURATION.md — do not commit real URLs or tokens.

KBAPP_API_BASE_URL=https://api.example.com
KBAPP_AUTH_TOKEN=
